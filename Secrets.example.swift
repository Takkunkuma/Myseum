// Template — copy to Sources/Config/Secrets.swift and fill in your values.
// Sources/Config/Secrets.swift is gitignored so your keys are never committed.
// Find these in the Supabase dashboard → Project Settings → API.

enum Secrets {
    static let supabaseURL = "https://YOUR-PROJECT.supabase.co"
    static let supabaseAnonKey = "YOUR-ANON-KEY"
}
